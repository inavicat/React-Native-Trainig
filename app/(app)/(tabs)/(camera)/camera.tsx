import { Text } from "react-native";

export default function CameraScreen() {
    return (
        // <Text style={{backgroundColor:'red',marginTop:100}}>CameraScreen</Text>
        <Text>CAM</Text>
    );
}